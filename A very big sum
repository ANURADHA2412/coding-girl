#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
     long int  a[10];
      long int n,i,sum=0;
    scanf("%ld",&n);
    for(i=0;i<n;i++)
        scanf("%ld",&a[i]);
    for(i=0;i<n;i++)
        sum=sum+a[i];
    printf("%ld",sum); 
    return 0;
}
