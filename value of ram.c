#include<stdio.h>
int main()
{
     int n, dis;
     printf("enter the value io ram entering");
     scanf("%d ,&n);
 if(n==1)
        dis=10/100 *450;
 else
       if =2/100 *450.;
 else
      if(n>2 && n<0)
      dis= 5/100 *450;
      printf ("the discount given to ram is %d",dis);
      return 0;
  }
