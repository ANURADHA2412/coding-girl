#include<stdio.h>
int main()
{
 int a,b,c;
 printf("enter the age of a,b,c);
 scanf("%d %d %d,&a &b &c);
 if(a<b && b<a)
 printf("A is youngest");
 else
 if (b,c && b<a)
 printf ("B is youngest");
 else
 printf("C is youngest");
 return 0;
 }
